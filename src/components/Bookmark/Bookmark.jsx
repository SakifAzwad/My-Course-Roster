/* eslint-disable react/prop-types */
/* eslint-disable react/jsx-key */

const Bookmark = ({x}) => {
    const {title}=x
    return (
        
    <>
        {title}
    </>
        
    );
};

export default Bookmark;